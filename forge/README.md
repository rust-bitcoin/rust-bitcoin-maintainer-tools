# Forge CLI tool

A tool for working with the rust-bitcoin forgejo instance.

## API Keys

An API key can be found in the web UI under `User Settings > Applications > Access tokens`. The token only needs the following permissions.

* read/write access to `repository`
* read access to `issue`
