# Rust Bitcoin Maintainer Tools CI Runner Container

This directory contains a Containerfile and documentation for building a container image that includes all dependencies needed to run the cargo-rbmt CI workflow.
